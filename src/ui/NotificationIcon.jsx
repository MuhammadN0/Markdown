import { HiBell } from 'react-icons/hi2';

function NotificationIcon() {
  return (
    <div>
      <HiBell style={{ color: `var(--white)`, cursor: 'pointer' }} size={20} />
    </div>
  );
}

export default NotificationIcon;
